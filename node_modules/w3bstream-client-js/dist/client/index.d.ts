/// <reference types="node" />
import { AxiosResponse } from "axios";
import { WSHeader, WSPayload, IW3bstreamClient } from "./types";
export declare class W3bstreamClient implements IW3bstreamClient {
    private _url;
    private _apiKey;
    private _DATA_PUSH_EVENT_TYPE;
    private _worker;
    private _publishIntervalMs;
    private _batchSize;
    private _maxQueueSize;
    queue: WSPayload;
    constructor(_url: string, _apiKey: string, options?: {
        enableBatching?: boolean;
        batchSize?: number;
        publishIntervalMs?: number;
        maxQueueSize?: number;
    });
    enqueueAndPublish(header: WSHeader, payload: Object | Buffer): boolean;
    publishDirect(header: WSHeader, payload: Object | Buffer): Promise<AxiosResponse>;
    stop(): void;
    private _startWorker;
    private _publishQueue;
    private takeFromQueue;
    private addToQueue;
    private _validateHeader;
    private _buildPayload;
    private _buildUrl;
    private _currentTimestamp;
    private _publish;
}
